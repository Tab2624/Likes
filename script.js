var count = 9
function addLikes(idToSelect){
    var elem = document.querySelector(idToSelect)
    count++
    elem.innerText = count + ' like(s)'
}

var count2 = 12
function addLikes2(idToSelect){
    var elem = document.querySelector(idToSelect)
    count2++
    elem.innerText = count2 + ' like(s)'
}


var count3 = 9
function addLikes3(idToSelect){
    var elem = document.querySelector(idToSelect)
    count3++
    elem.innerText = count3 + ' like(s)'
}